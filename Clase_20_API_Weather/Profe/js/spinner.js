export function spinner() {
    const spinner = document.createElement('h2')
    spinner.style ="display:flex; justify-content:center; alig-items:center; font-size:30px;"
    resultado.appendChild(spinner)
}